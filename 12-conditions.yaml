- name: conditions
  hosts: localhost
  tasks:
  - nmae: check roboshop user exists or not
    ansible.builtin.command: id roboshop
    register: user #user is variable name

  - nmae: print variable value
    ansible.builtin.debug:
      msg: "User output is {{user}}"